import type { Metadata } from "next";

// Rendered as a pane by `dashboard/layout.tsx`.
export const metadata: Metadata = { title: "Templates" };

export default function TemplatesPage() {
  return null;
}
