import type { Metadata } from "next";

// Rendered as a pane by `dashboard/layout.tsx`. The pane re-seeds its
// view/selection from the URL search params on the client.
export const metadata: Metadata = { title: "Progress" };

export default function ProgressPage() {
  return null;
}
